#!/usr/bin/env bash
set -euo pipefail

exec "${KATL_CONTAINER_RUNTIME:-docker}" run --rm \
    "${KATL_MKOSI_IMAGE:-localhost/katl-mkosi-builder:fedora-$("$(dirname "${BASH_SOURCE[0]}")/fedora-release")-go-squashfs}" \
    dnf5 "$@"
