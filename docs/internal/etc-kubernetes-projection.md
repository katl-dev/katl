# `/etc/kubernetes` Projection

This decision defines how kubeadm-owned state is made writable and persistent
on a runtime root where `/etc` is otherwise immutable and generated by confext.

## Decision

Katl uses a bind mount for `/etc/kubernetes`.

```text
source: /var/lib/katl/kubernetes/etc-kubernetes
target: /etc/kubernetes
type: none
options: bind,rw
```

The backing directory lives on the writable state partition. The target is the
only writable `/etc` subtree in the first implementation. Katl must not bind
mount all of `/etc`, and generated confext input must continue to reject
ownership of `/etc/kubernetes`.

## Ordering

The projection must become active after selected confext activation and before
any Kubernetes consumer starts:

```text
var.mount
katl generation selector creates /run extension activation links
systemd-sysext.service
systemd-confext.service
etc-kubernetes.mount
kubelet.service
kubeadm init/join automation
katl-kubeadm-ready.target
```

The mount unit should use ordering equivalent to:

```text
After=var.mount systemd-confext.service
Before=kubelet.service katl-kubeadm-ready.target
RequiresMountsFor=/var/lib/katl/kubernetes/etc-kubernetes
```

If kubeadm automation is represented by separate units, those units must also
order after `etc-kubernetes.mount`.

## Validation

Installer and first-boot validation must prove:

```text
/var is mounted from the installed state partition
/var/lib/katl/kubernetes/etc-kubernetes exists and is a directory
/etc/kubernetes exists as a mount point after projection
/etc itself is not a writable bind mount
confext does not contain /etc/kubernetes
kubelet and kubeadm automation units order after the projection
```

Validation should fail closed if `/etc/kubernetes` is already a non-empty
non-mount directory supplied by the root image or confext. The only acceptable
contents at the target before the bind mount are an empty directory or a
mountpoint placeholder created for this projection.

## Ownership Boundary

Kubeadm and kubelet own the projected subtree:

```text
/etc/kubernetes/pki
/etc/kubernetes/*.conf
/etc/kubernetes/manifests
```

Katl-generated kubeadm input should live under `/etc/katl`, for example
`/etc/katl/kubeadm-init.yaml` or `/etc/katl/kubeadm-join.yaml`, and kubeadm
automation should call kubeadm with those config paths. Kubeadm output remains
in projected `/etc/kubernetes`.

## Deferred Work

The exact unit file and `systemd-analyze verify` gate belong to the mount unit
implementation task. QEMU validation must prove the projection is visible before
kubelet and kubeadm automation start.
